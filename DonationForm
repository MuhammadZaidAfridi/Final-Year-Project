
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donation Form</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-6 rounded shadow-md w-full max-w-md">
        <h2 class="text-2xl font-bold mb-4">Please Donate today</h2>
        
        <!-- Donation Type Toggle -->
        <div class="flex mb-4">
            <button class="flex-1 py-2 text-center bg-gray-300 rounded-l">One off</button>
            <button class="flex-1 py-2 text-center bg-gray-200 rounded-r">Monthly</button>
        </div>
        
        <!-- Predefined Amount Buttons -->
        <div class="flex justify-around mb-4">
            <button class="bg-white py-2 px-4 rounded border">PKR</button>
            <button class="bg-yellow-400 py-2 px-4 rounded border">PKR</button>
            <button class="bg-white py-2 px-4 rounded border">PKR</button>
        </div>
        
        <!-- Custom Amount Input -->
        <div class="mb-4">
            <label for="customAmount" class="block mb-2">Choose Your own Amount (US$)</label>
            <input type="text" id="customAmount" placeholder="Placeholder" class="w-full px-4 py-2 border rounded">
        </div>
        
        <!-- Currency Dropdown -->
        <div class="mb-6">
            <label for="currency" class="block mb-2">Choose Your Currency</label>
            <select id="currency" class="w-full px-4 py-2 border rounded">
                <option value="PKR">Pkr</option>
                <option value="USD">USD</option>
                <option value="EUR">EUR</option>
                <!-- Add more currencies as needed -->
            </select>
        </div>
        
        <!-- Submit Button -->
        <div>
            <button class="w-full bg-yellow-400 py-2 rounded text-white font-bold">Donate</button>
        </div>
    </div>
</body>
</html>
    </div>   
</body>
</html>
